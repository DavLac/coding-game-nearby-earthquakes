apply plugin: "war"

bootWar {
    mainClassName = "fr.dla.app.earthquakes.EarthquakesApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
